define({
  initialize: function(app){

    /*
    var core = app.core,
        data = core.data,
        deferred = data.deffered;
    */
    
    console.log(app);

  },
  afterAppStart: function(app){

  }
});

