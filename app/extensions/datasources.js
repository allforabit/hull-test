/*
 * Wrap hull
 */

define(
  initialize: function(app) {

    //console.log(app);

    app.components.before('initialize', function(){
      console.log(app);

      if(this.datasources){
        
      }

      var api = app.core.data.hullApi.api;
      return ap.
    });

  }

});

